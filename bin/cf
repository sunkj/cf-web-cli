#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('new', '创建项目')
  .command('build', '编译项目')
  .command('generator', '生成模板')
  .parse(process.argv)


  
